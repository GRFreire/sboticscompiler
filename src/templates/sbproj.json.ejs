{
  "name": "<%= props.name %>",
  "version": "<%= props.version %>",<% if (props.description) { %>
  "description": "<%= props.description %>",<% } %>
  "main": "src/main.cs",
  "outputFolder": "out",<% if (props.license) { %>
  "checkForErrors": <%= props.hasDotnet %>,
  "license": "<%= props.license %>"<% } %><% if (props.author) { %>,
  "author": [
    "<%= props.author %>"
  ]<% } %>
}